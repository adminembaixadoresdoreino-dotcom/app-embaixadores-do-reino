spring:
  application:
    name: embaixadores-backend

  datasource:
    url: jdbc:postgresql://localhost:5433/embaixadores
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
  servlet:
    context-path: /api

logging:
  level:
    br.com.emanuel: DEBUG
    org.springframework.security: DEBUG
